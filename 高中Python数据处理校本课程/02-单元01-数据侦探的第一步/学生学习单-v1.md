# U1《数据侦探的第一步》学生学习单 v1.0

**姓名：** ____________　**班级：** ____________　**搭档：** ____________  
**单元任务：** 用 Python 制作一份“周数据小报”，回答一个简单的日常问题，并说明你的数据结论有什么边界。

> 你不是在完成一串语法题，而是在练习一条数据处理链：**问题 → 数据 → 程序 → 结果 → 解释与边界。**

## 任务 0：先做一名负责任的数据侦探

请选择一个主题：阅读分钟数、运动分钟数、睡眠时长、学习番茄钟次数，或教师提供的其他**模拟数据**。本单元不收集姓名、成绩、健康、家庭、精确位置或财务明细等敏感个人信息。你可以完全使用下面的模拟数据，也可以在教师允许的范围内改写为不含个人身份信息的记录。

| 我的问题 | 我需要的数据字段 | 我不需要/不应收集的数据 |
|---|---|---|
| 例：这一周我是否达到每天阅读 30 分钟的目标？ | 星期、阅读分钟数、目标分钟数。 | 姓名、读的具体书名、借阅地点。 |
| 我的问题： |  |  |

### 数据结论检查

请在运行程序前判断下列说法。

| 说法 | 你认为：可由一周数据支持 / 不能直接支持 | 原因 |
|---|---|---|
| 这一周有几天达到了 30 分钟阅读目标。 |  |  |
| 阅读超过 30 分钟一定会使所有科目成绩提高。 |  |  |
| 这一周的平均阅读时长是多少。 |  |  |
| 全校学生都喜欢阅读。 |  |  |

## 任务 1：预测后再运行

在 Python 编辑器中新建文件，输入或打开以下代码。先不要运行，先预测控制台会显示什么。

```python
print("欢迎来到数据侦探社")
print("今天我们让 Python 帮忙读懂一周记录")
```

| 我的预测 | 实际输出 | 是否一致？若不一致，哪里不同？ |
|---|---|---|
|  |  |  |

### 我今天学会了

请用自己的话补全句子：`print()` 的作用是：______________________________________________________。

## 任务 2：变量——给信息贴上可计算的标签

阅读下列代码，给每个变量填入对应的值和数据类型。

```python
reading_goal = 30
monday_minutes = 25
study_note = "今天读了科幻小说"
```

| 变量名 | 当前保存的值 | 我认为它是：数值 / 文字 | 为什么？ |
|---|---|---|---|
| `reading_goal` |  |  |  |
| `monday_minutes` |  |  |  |
| `study_note` |  |  |  |

### 改一改再运行

将变量改成你的主题。至少增加两项信息，然后用 `print()` 输出它们。

```python
# 在这里写下你的变量和输出
```

**预测：** 如果我把 `reading_goal` 从 `30` 改成 `45`，输出会如何变化？

________________________________________________________________________________

## 任务 3：条件——把规则写进程序

把生活规则补全：

> 如果今天的阅读分钟数 ________ 目标分钟数，就显示“今日达标”；否则显示“明天继续努力”。

观察并运行以下代码：

```python
reading_goal = 30
monday_minutes = 25

if monday_minutes >= reading_goal:
    print("今日达标！")
else:
    print("明天继续努力。")
```

### 边界实验

只改变 `monday_minutes` 的值，完成下表。

| `monday_minutes` | 你预测的输出 | 实际输出 | 解释 |
|---:|---|---|---|
| 29 |  |  |  |
| 30 |  |  |  |
| 31 |  |  |  |

请圈出代码的三个部分：**条件**、**条件为真时执行的代码块**、**条件为假时执行的代码块**。

## 任务 4：循环——处理一周记录

将一周数据放进一个列表。下面的数据是教学模拟数据，你可以在教师允许的范围内更改数值。

```python
weekly_minutes = [25, 35, 40, 15, 30, 50, 20]
```

### 先手算两轮

程序从第一个数字开始，依次把每一天的分钟数加到 `total_minutes` 中。请先手算前两轮。

| 第几轮 | 本轮 `minutes` | 旧的 `total_minutes` | 新的 `total_minutes` |
|---:|---:|---:|---:|
| 1 | 25 | 0 |  |
| 2 | 35 |  |  |

### 再完成程序

打开 `数据小报_起始代码.py`。你将完成以下内容：

| 程序需要完成的工作 | 你会用到的知识 |
|---|---|
| 逐天显示记录 | 列表与 `for` 循环。 |
| 计算一周总量 | 累加。 |
| 计算平均量 | 总量 ÷ 天数。 |
| 计算达标天数 | 条件判断与计数。 |
| 给出一条简单建议 | 用输出表达结果。 |

### 输出记录

| 指标 | 我的程序结果 | 我能解释它表示什么吗？ |
|---|---:|---|
| 一周总量 |  |  |
| 平均每天 |  |  |
| 达标天数 |  |  |
| 我的建议 |  |  |

## 任务 5：调试是一种数据侦探技能

程序报错并不等于“我不会编程”。一次调试应包含四步：**阅读现象 → 找到位置 → 做最小修改 → 再次运行。**

请记录一次你亲自遇到或从调试卡中解决的问题。

| 错误现象或报错提示 | 我怀疑的位置 | 我做的最小修改 | 修改后结果 |
|---|---|---|---|
|  |  |  |  |

### 调试自问

1. 报错信息中有没有指出行号或变量名？___________________________________________________
2. 我是否使用了中文标点、漏掉引号、括号、冒号或缩进？________________________________________
3. 我能否先用一个更小的数据例子手算一次？_________________________________________________

## 任务 6：画出我的 IPO 知识图

IPO 是“输入—处理—输出”的缩写。请画出你的数据小报程序，不要求画得漂亮，但必须画得清楚。你可以照下面的结构补充内容。

```text
┌────────────────┐       ┌──────────────────────────────┐       ┌────────────────────┐
│ 输入 Input      │  ───▶ │ 处理 Process                  │  ───▶ │ 输出 Output        │
│                │       │                              │       │                    │
│ 1. ___________ │       │ 1. 遍历一周数据               │       │ 1. 一周总量        │
│ 2. ___________ │       │ 2. ________________________  │       │ 2. 平均每天        │
│                │       │ 3. ________________________  │       │ 3. 达标天数        │
└────────────────┘       └──────────────────────────────┘       └────────────────────┘
```

请在图旁回答：如果把目标分钟数从 30 改为 45，图中的哪一部分会变化？为什么？

________________________________________________________________________________

## 任务 7：发布周数据小报

提交前，请和搭档交换文件完成试运行。搭档只需要输入或修改数据，不需要阅读你的全部代码。

| 试运行检查 | 我自己检查 | 搭档检查 | 需要修改的地方 |
|---|---|---|---|
| 文件可打开、可运行。 | □ | □ |  |
| 能显示 7 天记录。 | □ | □ |  |
| 能输出总量、平均值、达标天数。 | □ | □ |  |
| 改变一条数据后，结果会合理变化。 | □ | □ |  |
| 输出文字容易理解。 | □ | □ |  |
| 我能说清数据不能说明什么。 | □ | □ |  |

## 任务 8：调试与数据边界反思

请写 150—250 字，至少包含三部分：第一，描述一次你遇到的错误以及如何修复；第二，用你的程序结果写出一个有数据依据的发现；第三，写出一条这份数据**不能**说明的结论，或指出还需要什么数据才能说得更可靠。

> 可用句式：
>
> “我起初遇到的错误是……。我通过……发现问题并改为……。程序显示……，因此我可以说……。但这份数据只记录了……，没有记录……，所以我不能据此断言……。”

**我的反思：**

________________________________________________________________________________

________________________________________________________________________________

________________________________________________________________________________

## 提交清单

请将下列三份成果放入以“班级_姓名_U1”为名的文件夹。

| 成果 | 文件或形式 | 已完成 |
|---|---|---|
| 智能产品 | `week_report.py` 或你修改后的 `数据小报_起始代码.py`。 | □ |
| 知识图 | 本学习单的 IPO 图页，拍照或电子绘制均可。 | □ |
| 反思报告 | 本学习单任务 8 的反思。 | □ |

## 挑战任务（完成基础任务后任选）

| 挑战 | 要求 |
|---|---|
| A. 连续达标 | 计算最长连续达标天数，并解释你的方法。 |
| B. 改造主题 | 将阅读改为运动、番茄钟或教师提供的其他模拟数据，并修改全部输出文字。 |
| C. 函数封装 | 把“计算总量”或“计算达标天数”写成函数。 |
| D. 结果更易读 | 为输出增加标题、分隔线和格式化小数。 |
