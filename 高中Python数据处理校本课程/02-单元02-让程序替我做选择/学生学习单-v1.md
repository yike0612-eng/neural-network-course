# U2《让程序替我做选择》学生学习单 v1.0

**姓名：** ____________　**班级：** ____________　**搭档：** ____________  
**单元任务：** 制作“校园任务助手”。它根据模拟情境给出一条建议，并让别人能看懂、测试和质询你的规则。

> 程序不是替人做决定的机器。它只能按人写下的规则处理给定信息。你的任务是：把规则写清楚、测一测、再说明它的边界。

## 任务 1：把模糊建议改成可判断规则

下面几句话为什么不能直接交给程序？请圈出模糊词，并把其中一条改成有明确条件的规则。

| 原始建议 | 模糊在哪里？ | 我的可判断改写 |
|---|---|---|
| 天气不好就不要外出。 |  |  |
| 时间够就去运动。 |  |  |
| 没完成任务就先学习。 |  |  |

### 规则四要素

选择“校园课间活动建议助手”主题，填写至少三条规则。你也可以在教师允许下选择“图书馆阅读建议”“社团活动提醒”等低风险主题。

| 输入（程序需要知道什么） | 条件（怎样判断） | 输出（程序给什么建议） | 优先级理由 |
|---|---|---|---|
| 例：`task_done` | `task_done == False` | 先整理并完成当前任务。 | 必要任务优先。 |
|  |  |  |  |
|  |  |  |  |
|  |  |  |  |

## 任务 2：让条件变成真或假

设定以下变量：

```python
weather = "雨"
free_minutes = 20
task_done = True
```

请先判断下列表达式会得到 `True` 还是 `False`，再在 Python 中验证。

| 表达式 | 我的预测 | 实际结果 | 为什么？ |
|---|---|---|---|
| `weather == "雨"` |  |  |  |
| `free_minutes >= 30` |  |  |  |
| `task_done == False` |  |  |  |
| `weather == "雨" and free_minutes >= 10` |  |  |  |
| `weather == "晴" or free_minutes < 10` |  |  |  |
| `not task_done` |  |  |  |

### 边界值挑战

如果“少于 10 分钟”被看作时间短，请分别写出 9、10、11 分钟时你希望程序怎样判断。

| 可支配时间 | 我希望的建议类别 | 我的规则表达式 |
|---:|---|---|
| 9 分钟 |  |  |
| 10 分钟 |  |  |
| 11 分钟 |  |  |

## 任务 3：设计规则优先级

有时会出现多个条件同时成立。例如：`weather == "雨"`、`free_minutes == 5`、`task_done == True`。你的程序应该优先考虑天气，还是优先考虑时间？没有唯一答案，但你必须说清楚理由。

请把下列规则排序（1 表示最先检查）。

| 规则 | 排序 | 为什么放在这里？ |
|---|---:|---|
| 当前任务还未完成。 |  |  |
| 可支配时间少于 10 分钟。 |  |  |
| 天气是雨天。 |  |  |
| 可支配时间不少于 30 分钟，天气晴。 |  |  |
| 其他情形。 |  |  |

### 我的第一个函数

补全或改写以下函数。注意：函数内部用 `return` 把建议交还给外部；之后可以在外部用 `print()` 显示它。

```python
def suggest_activity(weather, free_minutes, task_done):
    if __________________________:
        return "先整理并完成当前任务；需要时可向同伴或老师求助。"
    elif ________________________:
        return "时间较短，建议喝水、整理物品或做短时放松。"
    elif ________________________:
        return "建议选择室内阅读、棋类或拉伸活动。"
    elif ________________________:
        return "可以考虑户外运动或较完整的兴趣活动。"
    else:
        return "可以校园散步、阅读或进行短时兴趣活动。"
```

请用一句话解释：为什么这里要用 `elif`，而不是写成多个互不相干的 `if`？

________________________________________________________________________________

## 任务 4：预测、运行、修订

运行你的函数之前，请先写出预期。运行后比较实际结果；不一致时不要立刻改很多行，先找出哪条规则被优先执行。

| 编号 | `weather` | `free_minutes` | `task_done` | 预期输出 | 实际输出 | 是否需要改规则？ |
|---:|---|---:|---|---|---|---|
| 1 | 晴 | 30 | True |  |  |  |
| 2 | 雨 | 20 | True |  |  |  |
| 3 | 晴 | 5 | True |  |  |  |
| 4 | 晴 | 30 | False |  |  |  |
| 5 | 雨 | 5 | True |  |  |  |
| 6 | 晴 | 10 | True |  |  |  |

### 调试记录

| 我看到的现象 | 我定位到的规则/代码行 | 我做的最小修改 | 再次运行后的结果 |
|---|---|---|---|
|  |  |  |  |

## 任务 5：用循环处理多组情境

下面的记录全部是教学模拟数据，不代表任何真实同学。阅读代码并补充循环，让程序依次为每条情境给出建议。

```python
situations = [
    {"weather": "晴", "free_minutes": 5, "task_done": True},
    {"weather": "雨", "free_minutes": 20, "task_done": True},
    {"weather": "晴", "free_minutes": 30, "task_done": False},
]

for situation in situations:
    advice = suggest_activity(
        situation["weather"],
        situation["free_minutes"],
        situation["task_done"],
    )
    print(______________________________)
```

请回答：循环每一次重复的是什么？`situation["weather"]` 取到的是什么？

________________________________________________________________________________

### 选做：统计一种建议出现次数

如果建议文字中有“室内”，就把 `indoor_count` 加 1。请补全代码或用自己的方式实现。

```python
indoor_count = 0
for situation in situations:
    advice = suggest_activity(
        situation["weather"],
        situation["free_minutes"],
        situation["task_done"],
    )
    if "室内" in advice:
        ______________________________

print(f"室内建议出现 {indoor_count} 次")
```

## 任务 6：画出规则决策流程图

请将你的规则画成流程图。矩形表示“输入或输出”，菱形表示“判断条件”；箭头必须表现规则顺序。请至少包含：三个输入变量、三个判断、一个默认输出和一个“不适用情形”。

```text
[输入：天气、可支配时间、任务完成情况]
                  ↓
          ◇ __________________？
          ↙是                    否↘
 [输出：________________]      ◇ __________________？
                                    ↙是            否↘
                         [输出：________]     ……………………
```

**不适用情形：** 我的程序不应替人决定的情况是：

________________________________________________________________________________

## 任务 7：规则公平性与边界反思

请写 150—250 字。你的反思至少回答：第一，你的程序依据哪些输入给建议；第二，哪一条规则最需要边界测试，为什么；第三，程序没有看到什么信息，在哪些情形下应由人而不是程序作最终决定；第四，如果继续改进，你会增加什么条件或说明。

> 可用句式：
>
> “我的程序读取……，先检查……，再检查……。我把……放在前面，因为……。当输入为……时，我发现……，于是把……改成……。但是程序没有考虑……，所以它不适合用来……。下一版我会……。”

**我的反思：**

________________________________________________________________________________

________________________________________________________________________________

________________________________________________________________________________

## 任务 8：同伴试运行与展示

与一位同伴交换程序和测试表。运行同伴程序时，不要只输入对方已经试过的数据；请自己提出一组新的边界或组合输入。

| 同伴姓名 | 我使用的新输入 | 输出结果 | 我提出的一个问题 | 同伴准备修改什么？ |
|---|---|---|---|---|
|  |  |  |  |  |

### 90 秒展示脚本

请按以下顺序展示：

1. 我的任务助手想解决的问题是：______________________________________________。
2. 它使用的三个输入是：______________________________________________________。
3. 我最重要的一条规则和优先级理由是：________________________________________。
4. 这是我的一组边界测试：____________________________________________________。
5. 程序不能替人决定的是：____________________________________________________。

## 提交清单

| 三个成果 | 提交形式 | 已完成 |
|---|---|---|
| 智能产品 | `campus_task_helper.py`，至少 1 个函数、3 个以上分支、6 组测试。 | □ |
| 知识图 | 规则决策流程图，可手绘拍照或电子绘制。 | □ |
| 反思报告 | 本学习单任务 7 的反思。 | □ |

## 挑战任务

| 挑战 | 要求 |
|---|---|
| A. 优先级对照 | 写两个不同优先级版本，找出一组会输出不同结果的输入并解释。 |
| B. 规则参数化 | 让“时间短”的阈值成为函数参数，而不是写死为 10。 |
| C. 无效输入 | 对 `weather` 既不是“晴”也不是“雨”的情形给出“请确认输入”的提示。 |
| D. 建议统计 | 处理 10 条以上模拟情境，统计每种建议出现次数并解释原因。 |
